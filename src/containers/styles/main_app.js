import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    main: {
        flex: 1
    },
    drawer: {
        shadowColor: '#000000',
        shadowOpacity: 0.8,
        shadowRadius: 3
    }
});
